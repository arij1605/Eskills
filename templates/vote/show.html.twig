{% extends 'base.html.twig' %}

{% block title %}Vote{% endblock %}

{% block body %}
    <h1>Vote</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdVote</th>
                <td>{{ vote.idVote }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ vote.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('vote_index') }}">back to list</a>

    <a href="{{ path('vote_edit', {'idVote': vote.idVote}) }}">edit</a>

    {{ include('vote/_delete_form.html.twig') }}
{% endblock %}
