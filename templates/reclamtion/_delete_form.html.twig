<form method="post" action="{{ path('reclamtion_delete', {'idRec': reclamtion.idRec}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamtion.idRec) }}">
    <button class="btn">Delete</button>
</form>
